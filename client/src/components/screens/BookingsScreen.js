import React from "react";
import BookingsScreenContainer from "../containers/BookingsScreenContainer";

const BookingsScreen = ({ navigation }) => (
  <BookingsScreenContainer navigation={navigation} />
);

export default BookingsScreen;
