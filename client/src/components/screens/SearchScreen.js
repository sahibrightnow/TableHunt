import React from "react";
import SearchScreenContainer from "../containers/SearchScreenContainer";

const SearchScreen = ({ navigation }) => (
  <SearchScreenContainer navigation={navigation} />
);

export default SearchScreen;
