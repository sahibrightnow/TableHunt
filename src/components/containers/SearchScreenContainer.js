import { Center, VStack } from "native-base";
import React from "react";

const SearchScreenContainer = ({ data }) => {
  return (
    <VStack space={5} py={10} px={5}>
      <Center>Search SCREEN</Center>
    </VStack>
  );
};

export default SearchScreenContainer;
