import {
  Center,
  VStack,
  HStack,
  Stack,
  Heading,
  Text,
  Box,
  AspectRatio,
  Image,
} from "native-base";
import React from "react";

const BookingsScreenContainer = ({ data }) => {
  return (
    <VStack space={5} py={10} px={5}>
      <Center>Bookings SCREEN</Center>
    </VStack>
  );
};

export default BookingsScreenContainer;
